---
datacenter:
  children:
    IOS:
      hosts:
        r1:
        r2:
        sw1:
        sw2:
      vars:
        ansible_connection: network_cli
        ansible_network_os: ios
        ansible_user: admin
        ansible_ssh_pass: cisco
        ansible_become: no
    
    #ACI:
      #hosts:
        #APIC1:
        #APIC2:

      #vars:
        #ansible_connection: network_cli
        #ansible_network_os: nxos
        #ansible_user: admin
        #ansible_ssh_pass: cisco
        #ansible_become: no
